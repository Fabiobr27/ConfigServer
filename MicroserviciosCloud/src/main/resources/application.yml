server:
  port: 8888
  
spring:
  cloud:
    config:       server:
        git:
          uri: https://github.com/Fabiobr27/ConfigServer.git
          default-label: main
          search-paths:
          - '*service'